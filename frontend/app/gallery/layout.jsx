export const metadata = {
  title: "Project Gallery",
  description: "Browse visual records of our executed commercial horticulture, institutional landscaping, and large-scale environmental projects.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}